extends layouts/main

block content
    .form-container
        form(action=routes.join, method="post") 
            input(type="text", name="name", placeholder="이름" required=true)
            input(type="emai", name="email", placeholder="이메일" required=true)
            input(type="password", name="password", placeholder="password" required=true)
            input(type="password", name="password2", placeholder="password again" required=true)
            input(type="submit", vlaue="회원가입")
        include partials/socialLogin